<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools" android:layout_width="match_parent"
    android:layout_height="match_parent" android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    android:paddingBottom="@dimen/activity_vertical_margin"
    tools:context="tainaleal.quicknotes.NoteDetailsActivity">


    <EditText
        android:layout_width="match_parent"
        android:layout_height="100dp"
        android:inputType="textMultiLine"
        android:ems="10"
        android:id="@+id/updateNote"
        android:gravity="top"
        android:layout_alignParentTop="true"
        android:layout_centerHorizontal="true" />

    <Button
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="@string/update_note_button"
        android:background="@drawable/red_button"
        style="@style/button_text"
        android:id="@+id/updateNoteButton"
        android:layout_below="@+id/updateNote"
        android:layout_alignRight="@+id/updateNote"
        android:layout_alignEnd="@+id/updateNote" />

    <Button
        style="?android:attr/buttonStyleSmall"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:background="@drawable/red_button"
        android:drawableStart="@drawable/ic_action_delete"
        android:drawableLeft="@drawable/ic_action_delete"
        android:id="@+id/deleteNoteButton"
        android:layout_alignTop="@+id/updateNoteButton" />

    <Button
        style="?android:attr/buttonStyleSmall"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:background="@drawable/red_button"
        android:drawableStart="@drawable/ic_navigation_arrow_back"
        android:drawableLeft="@drawable/ic_navigation_arrow_back"
        android:id="@+id/cancelUpdateButton"
        android:layout_alignTop="@+id/deleteNoteButton"
        android:layout_toRightOf="@+id/deleteNoteButton"
        android:layout_toEndOf="@+id/deleteNoteButton"
        android:layout_marginLeft="10dp"/>


</RelativeLayout>
